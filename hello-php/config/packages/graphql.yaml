overblog_graphql:
    definitions:
        schema:
            query: Query
            resolver_maps:
                - App\GraphQL\GraphQLResolverMap
        mappings:
            auto_discover: false
            types:
                -
                    type: graphql
                    dir: "%kernel.project_dir%/config/graphql/types"
                    suffix: ~
